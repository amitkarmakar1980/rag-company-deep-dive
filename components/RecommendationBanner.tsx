// This component is superseded by the ExecutiveSummarySection in components/report/ExecutiveSummary.tsx
// Kept as a no-op re-export to avoid breaking any stale imports.
export function RecommendationBanner(_props: { recommendation: string }) {
  return null;
}
